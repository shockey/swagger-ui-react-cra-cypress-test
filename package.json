{
  "name": "swagger-ui-react-cra-cypress-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "5.0.1",
    "swagger-ui-react": "latest"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "install-latest": "npm update swagger-ui swagger-ui-react",
    "test": "npm run install-latest && BROWSER=none run-p --race test:cypress start",
    "test:cypress": "cypress run"
  },
  "devDependencies": {
    "cypress": "^3.4.0",
    "npm-run-all": "^4.1.5"
  }
}
